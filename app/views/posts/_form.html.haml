= simple_form_for([@group, Post.new]) do |f|
  .form-group
    = f.input :title, input_html: { class: 'form-control', placeholder: "Title" }, label: false
  .form-group
    = f.input :body, as: :text, input_html: { class: 'form-control', placeholder: "Body"}, label: false
  .form-group
    = f.button :submit, "Create post", class: "btn btn-md btn-primary btn-block", id: "post_button"
